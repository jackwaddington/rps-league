TOKEN=your_token_here
BASE_URL=https://assignments.reaktor.com
# CRAWL_DELAY_MS: 0 = no delay between pages; 429s are handled automatically with 5s back-off
CRAWL_DELAY_MS=200
# DB_PATH must be inside a Docker volume so data survives rebuilds
DB_PATH=/app/data/games.db
